{
  "name": "move-up-server",
  "version": "1.0.0",
  "description": "Backend for the MOVE UP app — proxies AI calls to Anthropic (Claude) so the API key never reaches the browser.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.2.0"
  }
}
